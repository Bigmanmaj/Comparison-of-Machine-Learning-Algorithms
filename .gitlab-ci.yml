image: gcc:latest

before_script:
  - apt-get update && apt-get install -y cmake

build:
  script:
    - cmake -B build
    - cmake --build build

test:
  script:
    - cmake -B build
    - cmake --build build
    - ctest --test-dir build --output-on-failure